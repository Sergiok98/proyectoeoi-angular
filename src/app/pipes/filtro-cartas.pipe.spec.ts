import { FiltroCartasPipe } from './filtro-cartas.pipe';

describe('FiltroCartasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCartasPipe();
    expect(pipe).toBeTruthy();
  });
});
